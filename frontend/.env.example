# Copiar a `.env`. Vite expone al navegador solo las variables con prefijo VITE_.

# URL base del backend FastAPI
VITE_API_URL=http://localhost:8000

# Client ID de Google (ver "Login con Google" en el README).
# Si queda vacío, el botón de Google no se muestra.
VITE_GOOGLE_CLIENT_ID=
