# Copiar a `.env` (config.py lo carga con python-dotenv).

# Client ID de Google (el mismo que usa el frontend).
# Si queda vacío, /auth/google responde 503.
GOOGLE_CLIENT_ID=

# En desarrollo los valores por defecto de config.py alcanzan:
# SECRET_KEY=
# DATABASE_URL=
